import { StyleSheet } from "react-native";

export const AddBoardStyles = StyleSheet.create({
    addBoardForm: {
        flexDirection: 'column',
        width: '100%',
        alignItems: 'center'
    }
});
