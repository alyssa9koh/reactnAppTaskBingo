export const DEFAULT_SIZE = 5;

export const MIN_SIZE = 3;
export const MAX_SIZE = 6;

export const DEFAULT_TITLE = 'My Bingo Board';
export const DEFAULT_TASKS = Array(DEFAULT_SIZE ** 2).fill('default task');
export const DEFAULT_PRESSED_SQUARES = Array(DEFAULT_SIZE ** 2).fill(false);

export const BOARD_UUID_LIST_KEY = 'user_boards';
